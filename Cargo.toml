[package]
name = "neonprime"
version = "3.0.0"
edition = "2021"
description = "A holographic system control deck for Windows. One machine, three modes — it counts all the way to three."
authors = ["Ethan Belanger <belangerethan@gmail.com>"]
license = "MIT"

[dependencies]
slint = "1"
sysinfo = "0.33"
nvml-wrapper = "0.10"

[build-dependencies]
slint-build = "1"
